#include <iostream>
#include <math.h>
 using namespace std;
 int main ()
 {
     float x, a, f;
     for (x=-3; x<=2; x+=0.3)
     {if (fabs(sin(x))>1/2)
     f=pow(x+a,2);
     else
     f=x+1;
    cout<<"\n x="<<x<<"\t f="<<f;
     }
     
 }
